# CLAUDE.md — Order Entry System

> **Project constitution.** Read this before every task and follow it exactly. Never introduce a pattern, library, or table not described here. If anything is ambiguous, STOP and ask.

> This is **one of two separate apps.** This app owns **orders + operations tracking**. A second app — the **Embroidery System** (separate repo, separate DB) — *pulls* order data from here read-only to run stock/demand. **This app never manages stock, vendors, or procurement, and never calls the Embroidery System.**

---

## 1. What this is
Standalone **order-entry & operations-tracking** ERP for a fabric / embroidery house (LD Silk Mills). It is the **system of record for customer orders** and their **7-stage TAT (turnaround) tracking**, and it exposes a secured read API that the Embroidery System consumes.

**Modules (sidebar):** Dashboard (analytics) · New order · Orders · Order status · Operations · Trash · Settings.

**The order/design lifecycle** — every design line item is in exactly one of three states:
- **Active** — normal; counts in totals, status, tracking, exports.
- **Cancelled** (`is_cancelled`) — the customer cancelled it. Stays **visible, struck-through**; excluded from totals/status/tracking; **reversible** (§6).
- **Deleted / trashed** (`is_deleted`) — entered by mistake. **Hidden** from every normal view; recoverable only from the **Trash** page; **reversible**, and can be **permanently purged** from Trash (§6).

**Roles:** `ADMIN` (always full — settings & users) · `SALES` · `OPS` · `VIEWER`. *(`MANAGER` existed from migration 0003 and was **removed in 0006** — its grants were identical to ADMIN's, so it was a duplicate access level.)* **Each role's access is an admin-editable Role × Capability matrix** (Settings → Access, stored in `role_permissions`). Capabilities: `orders.view`, `orders.edit`, `operations.view`, `operations.edit` (defined in `lib/rbac.ts`). **ADMIN is always full and never stored/editable** (can't be locked out). *(Enforcement: a role's caps are resolved into the session JWT at login — so Access changes apply on the user's **next login** — and both the edge middleware (`canAccessPath(role, caps, path)`) and the write APIs (`requireCapability`) check them. Settings/user management stays ADMIN-only, not a capability.)*

## 2. Tech stack — LOCKED
Next.js 15 (App Router, TS strict) · **Supabase Postgres** (`postgres` / postgres.js) · Drizzle + drizzle-kit · NextAuth/Auth.js v5 (email+password **and Google OAuth**; roles; JWT sessions, no DB adapter) · Tailwind v4 + shadcn/ui (base-ui) · TanStack Query · **Recharts** (dashboard charts) · **next-themes** (light/dark via `data-theme`) · SheetJS (`xlsx`, reserved) · Vercel.
**All tables live in the `ld_order_entry` schema, never `public`** — the Supabase project *LD Silk Mills* is shared with other apps, and Supabase's Data API publishes only `public`, so order data is unreachable from it. `db/schema.ts` uses `pgSchema("ld_order_entry")`, so Drizzle emits fully-qualified names and nothing depends on `search_path`.
Not allowed: Neon, Prisma, any second UI kit, raw `pg` in app code.

## 3. Locked decisions
1. **`order_no` is TEXT, user-entered, UNIQUE** (e.g. `LKD-08-25-003`); validate duplicates on entry; never auto-number.
2. **Orders = one header + fabric blocks → many line items**, with pricing: `rate` per fabric, generated `line_total = qty_mtr * rate`; order grand total is **derived**, never stored.
3. **Operations tracking is per LINE ITEM** through 7 stages — `order_entry`, `stock_checking`, `rolling_checking`, `challan`, `bill`, `dispatch`, `received_lr` — each with planned/actual datetime, done flag, delay (minutes). Gating (§6): `order_entry` is the **initial step** (always editable); `stock_checking` is **locked until order entry is done**, then a 3-way gate (**Pending / In stock / Out of stock**) where only **In stock** unlocks the five stages after it — which then complete in **any order**. Un-ticking is always allowed (no downgrade block).
4. **Fabric & design are free text with autocomplete.** This app has **no product catalog.** Party / sales / agent / haste / transport / fabric autocomplete come from `lookup_values`; design suggestions come from past line items (logged in `design_database`, §5).
5. **Operations status** (derived per line: COMPLETED / PARTIALLY COMPLETED / PENDING; plus the order-level derived **CANCELLED**) is this app's status. **Inventory levels, demand, samples, procurement and fulfilment are NOT this app's concern** — that's the Embroidery System. The *only* stock concept here is the per-line **In stock / Out of stock gate** on the `stock_checking` stage — an operational checkpoint ("can this line proceed?"), **not** stock/inventory management.
6. **Exposes a secured read API** (`/api/export/orders`, §7) for the Embroidery System to pull orders incrementally. This app is the source; it never pulls from Embroidery.
7. **Cancellation is reversible and stays on record.** Cancel/restore a single design or a whole order; cancelled designs render struck-through and drop out of totals/status/tracking but are never removed. An order is **CANCELLED** (derived) when it has ≥1 line and **every non-deleted line is cancelled**. Reuses `order_line_items.is_cancelled` — no new table.
8. **Delete is a reversible SOFT delete → Trash** (never a silent hard delete from a list). Delete/restore a single design or a whole order; deleted designs are hidden everywhere and recoverable from **Trash**, which also offers **permanent purge**. An order is **deleted** (derived — no flag on the header) when **every line is soft-deleted**; deleting an order's last active design therefore removes the order to Trash. Uses `order_line_items.is_deleted` (migration 0005).

## 4. Folder structure
```
order-entry-system/
  app/
    (auth)/login/       page.tsx (split-screen) · login-form.tsx · login-theme-toggle.tsx
    (app)/              authenticated shell: page.tsx (Dashboard) · orders/ · order-status/ · tracking/ · trash/ · settings/
    api/                route handlers (JSON). orders/ · orders/[id]/{cancel,delete,tracking,lines/[lineId]} ·
                        order-status/ · tracking/stage · trash · dashboard · reports/monthly ·
                        export/orders · lookups · designs · design-database · stages · users ·
                        access · health · auth/[...nextauth]
  components/           app-shell/ · orders/ · order-status/ · tracking/ · dashboard/ · trash/ · settings/ · ui/
                        order-status/ also holds the TRACKING view shared by both list screens:
                        order-tracker · tracker-detail · stage-cell · quality-groups · view-switch
                        ui/ shared primitives: table · stat-card · h-scroll (top scrollbar) · pager
  lib/                  db.ts · auth.ts · auth.config.ts · workflow.ts (stage + status + derivations) · rbac.ts ·
                        api.ts (guards) · validation.ts (zod) · orders.ts (client types) ·
                        order-status.ts (types + computeStages/aggregateOrderGroups) ·
                        order-status-query.ts (loadOrderStatus) · dashboard.ts ·
                        dashboard-query.ts (loadDashboard) · monthly-report.ts · months.ts
  db/                   schema.ts (pgSchema "ld_order_entry") · migrations/ (0000 baseline +
                        _archive_neon_public/) · seed.ts · load-env.ts · copy-to-supabase.ts ·
                        clone-to-dev.ts (fills the dev schema for local work)
  middleware.ts         edge auth + canAccessPath
```

## 5. Database schema — CANONICAL
UUID PKs (`gen_random_uuid()`); `TIMESTAMPTZ DEFAULT now()`; quantities `DECIMAL(10,2)`. **`order_no`, `quality`, `design_no`, `challan_no`, `lot_no` are ALWAYS text.** No new tables were added for cancellation or deletion — both are per-line booleans.

### `users`
- `id` UUID PK · `email` UNIQUE NOT NULL · `password_hash` (**nullable** — Google-only users have none) · `name` · `role` (ADMIN|SALES|OPS|VIEWER, default VIEWER; MANAGER was added in *migration 0003* and dropped in *migration 0006*) · `is_active` BOOL DEFAULT TRUE · `created_at`

### `role_permissions`  (the admin-editable access matrix — Settings → Access)
- `id` UUID PK · `role` user_role · `capability` VARCHAR(40) (`orders.view`|`orders.edit`|`operations.view`|`operations.edit`) · `allowed` BOOL DEFAULT FALSE · `updated_at`
- **UNIQUE (`role`, `capability`)**. ADMIN is never stored (always full). Seeded to match the code defaults (`DEFAULT_ROLE_CAPS` in `lib/rbac.ts`); *migration 0004*.

### `customer_orders`
- `id` UUID PK · `order_no` VARCHAR(50) UNIQUE NOT NULL · `order_date` DATE NOT NULL · `party_name` VARCHAR(200) NOT NULL · `sales_person` VARCHAR(100) · `agent` VARCHAR(120) · `haste` VARCHAR(120) · `transport` VARCHAR(120) · `challan_no` VARCHAR(100) · `lot_no` VARCHAR(100) · `department` VARCHAR(40) DEFAULT 'LD' · `remarks` TEXT · `created_by` VARCHAR(120) · `created_at`, `updated_at`
- INDEX (`party_name`); INDEX (`order_date`)
- `crr_customer_id` INT — the CRR customer this order's party resolves to, or NULL when it could not be established **deterministically**. Never guessed. INDEX. *(migration 0001)*
- `party_name_original` VARCHAR(200) · `haste_original` VARCHAR(120) — what the operator actually typed, captured **before** the name was normalised to a CRR spelling. NULL = never normalised. Written once and never overwritten, so a re-run of the linker cannot lose the first value. **This is the audit trail: an order can always be shown, or restored, exactly as written.** *(migration 0001)*
- **There is NO `is_cancelled` / `is_deleted` on the header** — order-level CANCELLED and deleted are **derived** from the lines (§6).

### `order_line_items`
- `id` UUID PK · `order_id` UUID FK → customer_orders (cascade) · `quality` VARCHAR(100) NOT NULL (the fabric) · `design_no` VARCHAR(100) NOT NULL · `qty_mtr` DECIMAL(10,2) NOT NULL · `rate` DECIMAL(10,2) · `line_total` DECIMAL(12,2) GENERATED ALWAYS AS (`qty_mtr` * `rate`) STORED (**never write directly**) · `is_cancelled` BOOL DEFAULT FALSE (§6 cancellation) · `is_deleted` BOOL NOT NULL DEFAULT FALSE (*migration 0005* — soft-delete/Trash, §6) · `remarks` TEXT · `created_at`, `updated_at`
- INDEX (`order_id`); INDEX (`quality`, `design_no`)
- **`is_cancelled` vs `is_deleted` are independent.** A line can be both (cancel then delete); when restored from Trash it returns struck-through. Read paths that show data filter `is_deleted = false`; totals/status further filter `is_cancelled = false`.

### `workflow_stages`  (seed the 7 — also the Time-tracking / SLA config)
- `stage_key` VARCHAR(40) PK · `label` VARCHAR(60) NOT NULL · `sort_order` INT NOT NULL (1..7) · `planned_offset_days` INT NOT NULL DEFAULT 1 (**SLA**: days from `order_date` to this stage's planned deadline; edited in Settings → Time tracking) *(migration 0001)*

### `line_stage_progress`
- `id` UUID PK · `order_line_item_id` UUID FK → order_line_items (cascade) · `stage_key` FK → workflow_stages · `planned_at` TIMESTAMPTZ · `actual_at` TIMESTAMPTZ · `is_done` BOOL DEFAULT FALSE · `delay_minutes` INT · `stock_status` VARCHAR(20) (**only on the `stock_checking` row**: `in_stock` | `out_of_stock` | NULL — §6; *migration 0002*) · `updated_by` · `updated_at`
- **UNIQUE (`order_line_item_id`, `stage_key`)**; INDEX (`order_line_item_id`)

### `crr_customers`  (local copy of the CRR customer master's alias list)
- `id` UUID PK · `customer_id` INT (CRR's numeric id; **not unique** — one customer has many spellings) · `alias` VARCHAR(120) (CRR's branch/route marker) · `full_raw_name` VARCHAR(250) (the spelling exactly as CRR holds it) · `display_name` VARCHAR(250) (the tidiest spelling for that customer, branch tags stripped) · `canon` / `tight` VARCHAR(250) (precomputed match keys) · `created_at`
- **UNIQUE (`customer_id`, `full_raw_name`)**; indexes on `canon`, `tight`, `customer_id`. Loaded by `db/load-crr-customers.ts`; re-runnable when CRR sends a fresh export. *(migration 0001)*

### `lookup_values`  (autocomplete sources — the Dropdown Master)
- `id` UUID PK · `category` VARCHAR(30) NOT NULL (PARTY|SALES_PERSON|AGENT|HASTE|TRANSPORT|FABRIC) · `value` VARCHAR(200) NOT NULL · `is_active` BOOL DEFAULT TRUE · `crr_customer_id` INT (the CRR customer this spelling resolves to; drives the **In CRR** badge — only ever set for PARTY and HASTE) · INDEX (`category`), INDEX (`crr_customer_id`) *(migration 0002)*

### `design_database`  (log of every fabric+design used — powers design autocomplete + a browsable history)
- `id` UUID PK · `created_at` · `order_id` UUID FK → customer_orders (**ON DELETE SET NULL**) · `order_no` VARCHAR(50) NOT NULL (denormalized so it survives order deletion) · `fabric_name` VARCHAR(100) NOT NULL · `design_no` VARCHAR(100) NOT NULL
- **UNIQUE (`order_no`, `fabric_name`, `design_no`)**; INDEX (`fabric_name`); INDEX (`design_no`). Not touched by cancel/soft-delete (it's a persistent autocomplete history).

## 6. Business rules

### Orders & pricing
- `order_no` unique; reject duplicates with a clear message (`/api/orders/check-no` for live validation).
- `line_total` is generated; order grand total derived (Σ line_total over the shown set).
- On order save, **create the 7 `line_stage_progress` rows per line** (all `is_done=false`). Each stage's `planned_at` is **SLA-driven**: `order_date` 00:00 (UTC) + that stage's `planned_offset_days`. Planned dates are config-driven — completing a stage never rewrites any `planned_at`.
- **Edit an order:** replace its line items, but **preserve `line_stage_progress`** for lines that still match on (fabric + design + qty); create fresh stage rows only for genuinely new lines. Edit only reconciles **non-deleted** lines, so trashed lines survive an edit untouched. If the **order date changes**, kept lines' `planned_at` shift to the new SLA (`plannedAtForOffset` in `lib/workflow.ts`) so the order isn't "overdue" against its old date.

### Stage completion — STOCK-ONLY gating
(all in `lib/workflow.ts`, ONE transaction; a rule violation throws `WorkflowError` → the stage API returns **409** with the message):
- `order_entry` has **no prerequisite** (the initial step). `stock_checking` is **locked until `order_entry` is done** (no change of any kind before then). The five stages **after** `stock_checking` can be completed **only once stock is `in_stock`** — then in **any order** (e.g. challan without rolling).
- **`stock_checking` gate** — outcome stored in `stock_status`: `in_stock` **completes** the stage (unlocking downstream); `out_of_stock` records the block; `NULL` = undecided. Only `in_stock` counts as done; while `out_of_stock`/`NULL` the later stages stay **locked**.
- **Un-ticking is always allowed** (no downgrade block). Reverting stock away from `in_stock` does **not** auto-clear already-done downstream stages — they stay done and the line drops to PARTIALLY (the UI warns; the earlier auto-cascade was reverted).
- On complete: set `actual_at` (client value or now), `is_done=true`, `delay_minutes = round((actual − planned)/60000)`. Un-tick clears actual/done/delay. Recompute the line's operations status.
- **Cancelled or deleted lines cannot be stage-edited** — `PATCH /api/tracking/stage` returns **409** ("This design is cancelled." / "…has been deleted.").
- **Bulk "check all"** (per stage column) marks all lines done for that stage but **skips lines whose stock isn't `in_stock`** (reporting how many were skipped); `order_entry`'s applies to all. `stock_checking` has **no** header check-all — it's a per-line dropdown. Header "all done" is measured over *in-play* (completable-or-done) lines so out-of-stock lines don't block un-check-all.

### Operations status (derived; `lib/workflow.ts`)
- Per line: **all 7 done → COMPLETED**; **at least one of the 5 post-stock stages (`rolling_checking`, `challan`, `bill`, `dispatch`, `received_lr`) done → PARTIALLY COMPLETED**; otherwise (nothing done, or only `order_entry` / `stock_checking`) → **PENDING**. Completing only order entry + stock checking is *not* partial — it stays PENDING (they're preliminary). (`out_of_stock` leaves stock not-done → post-stock stages can't be done → the line stays PENDING, never COMPLETED.)
- Order-level status = roll-up of its **active** (non-cancelled, non-deleted) lines via `computeOrderStatus`, **except**: if the order is fully cancelled it is **CANCELLED** (`isOrderCancelled(total, cancelled)` = `total>0 && cancelled===total`, over non-deleted lines). `CANCELLED` is a client-side status value (`components/ui/status-badge.tsx` and the `OperationsStatus` union in `lib/workflow.ts`); it is **order-level only**, never stored.

### Cancellation (reversible; §3.7)
- `PATCH /api/orders/:id/cancel` `{ line_id?, cancelled }` — `line_id` set = one design; omitted/null = **all** the order's lines. `orders.edit`-gated. Bumps `customer_orders.updated_at` so the incremental export re-emits.
- Cancelled lines stay **visible, struck-through**; excluded from qty/amount/status/tracking. A fully-cancelled order still shows its (cancelled) lines' totals so the row isn't ₹0.
- Whole-order **Cancel** goes through a confirm dialog; **Restore** is immediate. Whole-order restore un-cancels **all** lines (there's no per-line cancel-scope stored — a known, recoverable limitation).

### Soft-delete + Trash (reversible; §3.8)
- `PATCH /api/orders/:id/delete` `{ line_id?, deleted }` — same shape as cancel; sets `is_deleted`. `orders.edit`-gated; bumps `updated_at`.
- Deleted lines are **excluded from every read path**: Orders list (and the fully-deleted order drops out via an `EXISTS(non-deleted line)` filter so pagination stays correct), order detail, edit form, Order status, Operations tracking, the Dashboard analytics, and all counts/summaries. They surface **only** in Trash.
- **Trash** (`/trash`, `GET /api/trash`, `orders.edit`-gated) lists two groups: **deleted orders** (every line deleted — one aggregated card) and **deleted designs** (individually-deleted lines in still-active orders). Each has **Restore** and **Delete permanently**.
- **Permanent purge:** `DELETE /api/orders/:id` (whole order, hard cascade — **guarded to refuse a live order**: only a fully soft-deleted order can be purged) and `DELETE /api/orders/:id/lines/:lineId` (one line, guarded to only purge an already-`is_deleted` line, and the guard is in the DELETE's WHERE to be atomic).

## 7. Export API (consumed by the Embroidery System **and SCOT**)
- `GET /api/export/orders` — auth via a static API key in a request header (`x-api-key`). Read-only. Sits **outside** the user-session middleware (excluded by `middleware.ts`). **Two consumers, two keys**, so either can be rotated or revoked alone: `EXPORT_API_KEY` (Embroidery System) and `EXPORT_API_KEY_SCOT` (SCOT, the sales-coordinator dashboard). Both are compared in constant time with no early exit.
- Query: `updated_since` (ISO timestamp) for incremental sync (INCLUSIVE — Embroidery dedupes on stable ids, so the boundary record may repeat); pagination (`page`, `limit`).
- Returns each order with header fields + line items carrying **stable ids** (`order.id`, `line.id` → Embroidery's `external_ref`): `order_no`, `order_date`, `party_name`, `sales_person`, `department`, and per line `quality`, `design_no`, `qty_mtr`, **`is_cancelled`**, **`is_deleted`** (emitted, not hidden, so Embroidery can remove trashed lines on its side), plus the line's operations status. **Pricing is per-consumer**: `rate` and `line_total` are emitted ONLY to SCOT (revenue drives its client categorisation) and never to Embroidery, which never asked for them. Gated on which key matched — see `CONSUMERS` in the route.

### CRR customer identity (party & haste)
`party_name` and `haste` both hold **company names**. Both are matched against the local `crr_customers` copy of the CRR master by `lib/crr-match.ts`, which offers three keys in descending confidence: exact spelling → `crrCanon()` (SCOT's own rules) → `crrTight()` (also folds internal punctuation and trailing plurals, which is what recovers most of our misses). **A key that resolves to more than one CRR customer is skipped, never guessed** — an unresolved order is a normal state, not an error.

- `db/load-crr-customers.ts` — loads/refreshes the CRR alias export.
- `db/link-orders-to-crr.ts` — links orders and normalises spellings. **Dry run by default; `--apply` to commit.** Preserves originals.
- `db/sync-crr-dropdowns.ts` — tags dropdown values with their CRR customer and adds CRR customers not already covered. Never deletes or rewrites an existing value, and never adds a customer an existing value already resolves to (that would refill the list with near-duplicates).

**Branch tags are always stripped from a display name** (`(N)`, `(JOB)`, `(AITK)` are route markers, not part of the company). Where stripping would collide two customers, the tag is kept on the later one so the dropdown is never ambiguous.

### SCOT and customer-name identity
SCOT resolves our free-text `party_name` to its own customer master (CRR) using an alias table plus a canonicalising function that strips trailing dots, trailing bracket groups and one trailing corporate suffix. **Therefore: pass party names through VERBATIM.** Never trim, case-fold, expand or otherwise "clean" a name on the way out — that table is built from the raw historical spellings, so a tidied string arrives as an unknown NEW variant and fragments the customer's history. Equally, **never silently rename a party on existing orders**; old orders must keep the name they were created with. Tidying duplicate values in the Dropdown Master is fine and useful (it stops new variants being coined), but it must not rewrite `customer_orders.party_name`.

**Open with SCOT:** our `haste` field also holds company names (2,275 in the Dropdown Master — it is the "through/care of" party, not an urgency level), and the export does not send it. SCOT's spec assumes one customer name per order. Currently only ~10 of 222 orders populate it, but this needs agreeing before it grows.

## 8. Conventions
- API under `/app/api`, JSON, auth on every route (the export route uses the API key). Error `{ error }`, success `{ data }`. Guards: `requireCapability` / `requireAnyCapability` / `requireRole` in `lib/api.ts`.
- Operations-stage logic and the order-level derivations (`computeOrderStatus`, `isOrderCancelled`, `isOrderDeleted`) live only in `lib/workflow.ts`. DB only via Drizzle through `lib/db.ts`. No raw SQL except migrations.
- `order_no` / `quality` / `design_no` always text — never `parseInt`/`Number()`.
- Fabric/design free text with autocomplete; never block an unknown value.
- `line_total` generated — never write it; never store the grand total.
- Validation: all write payloads go through zod schemas in `lib/validation.ts`.
- Migrations: drizzle-kit, zero-padded sequential. `npm run db:migrate` works again as of the Supabase move (see Implementation notes). Migrations use `DIRECT_URL`, never the pooler.
- UI: sentence case; tables primary; semantic design tokens (§9); light **and** dark.

## 9. Design tokens
- **Intent (spec):** `--primary` `#4F46E5` · `--primary-dark` `#4338CA` · `--success` green · `--warning` amber · `--danger` red. The **implemented** palette lives in `app/globals.css` (CSS variables + Tailwind v4 `@theme`, flipped by `[data-theme="dark"]`): accent `#4f46e5` (light) / `#818cf8` (dark); semantic tokens `text-ink`/`ink-soft`/`ink-muted`, `bg-surface`/`surface-2`/`inset`, `border-line`/`line-strong`, `success`/`warning`/`danger`, `.num` (tabular figures in the platform UI sans — it used to force JetBrains Mono, which aligned perfectly but read as code on screens that are mostly money and quantities. It cannot inherit General Sans: that face has proportional digits and no `tnum`, so rupee columns would stagger. See the comment in `globals.css`.). Prefer `text-ink` over literal `black` so dark mode works. Fonts: `font-display` (Clash Display), `font-body` (General Sans), `font-mono` (JetBrains Mono) — wired in `app/fonts/`.
- **Operations stage colours** (per-stage dot): order_entry indigo, stock_checking blue, rolling_checking amber, challan rose, bill emerald, dispatch violet, received_lr cyan. Reused for the Dashboard pipeline bars and Order-status stage dots (always shown **with a stage-name label**, since these hues are close under colour-blindness).
- **Operations tracking — per-stage cell status colours** (the cell tint *is* the status; planned/actual dates hidden, shown on hover): Done on-time → green (no pill); Done late → amber + `+Xm` pill; Live (first not-done) → indigo/accent; Overdue (live & past planned) → red; Out of stock → red "Blocked"; Locked/Pending → grey. Derived by `cellState()` in `tracking-board.tsx`.
- **Status pill colours** (`status-badge.tsx`): COMPLETED green · PARTIALLY COMPLETED amber · PENDING grey · **CANCELLED danger**. Cancelled designs are struck-through (`text-ink-muted line-through`).
- **Login** (`app/(auth)/login`): split-screen — a brand panel that is **dark in BOTH themes** (it is the near-black the app shell uses, so signing in previews the product) beside a theme-aware form panel. **Restraint is the spec.** The brand panel carries exactly three decoration layers: one smooth vertical ground, one large soft light low on the panel, one hairline seam. A 45° crosshatch "weave" texture and a second competing glow were both tried and **reverted** — at real screen sizes the crosshatch read as scratchy moiré and two glows read as a gradient demo. No gradient headline, no glowing eyebrow pill, no lit gradient button: a flat accent primary with a soft shadow, and a Google button whose border simply warms. The form column is capped at **380px with no card, border or shadow** — whitespace frames it; boxing it is what made an earlier pass read as a template. Uses app tokens/fonts, not external Google fonts.

## 10. Do / Don't
DO: read this first; keep stage/status/derivation logic in `workflow.ts`; treat order/fabric/design as text; filter `is_deleted=false` on read paths and `is_cancelled=false` on totals/status; ask before adding anything not listed.
DON'T: manage stock/vendors/procurement here; auto-number `order_no`; write `line_total`; store an order-level cancelled/deleted flag (both are derived); hard-delete from a list (soft-delete → Trash); block unknown fabric/design; call the Embroidery System.

## 11. Build order (history)
- **OE-P0** — scaffold + Drizzle + schema + migration + seed + health (originally on Neon).
- **OE-P1** — auth + role middleware + app shell.
- **OE-P2** — order entry form (rich) + Orders Dashboard.
- **OE-P3** — Operations tracking (7-stage workflow).
- **OE-P4** — secured export API.
- **OE-P5** — Settings / master data (Dropdown Master, Design Database, Time-tracking SLA, Users, Access).
- **OE-P6** — Analytics dashboard + Order Status board + `status-drawer`.
- **OE-P7** — mobile-responsive shell + operations rework (stock gate, colour-coded grid).
- **OE-P8** — Google sign-in · MANAGER role · list filters + CSV · mobile card views · optimistic tracking · stock-only gating.
- **OE-P9** — **reversible order/design cancellation** (`is_cancelled`, CANCELLED status, expandable Orders table with per-design cancel).
- **OE-P10** — **reversible soft-delete + Trash** (`is_deleted`, migration 0005, `/trash`, permanent purge, per-design delete in the expandable Orders table; Trash nested under Settings in the nav).
- **OE-P12** — **Neon → Supabase migration**: all tables moved into the `ld_order_entry` schema of the shared *LD Silk Mills* project; `@neondatabase/serverless` replaced by postgres.js; migration history rebuilt from a clean baseline. Data moved with `db/copy-to-supabase.ts` (Neon was PG 18.4 vs Supabase 17.6, so `pg_dump` was unusable — Postgres restores forwards only).
- **OE-P13** — **performance + order tracking + login**: the Dashboard's connection-pool stall (see the ⚠️ fan-out note), SQL-side roll-ups and an SSR prefetch; server-side grouping for Order status; the **order tracking view** on both Orders and Order status; `HScroll` and `Pager` shared across every wide/paginated table; `.num` moved off JetBrains Mono; and the login screen rebuilt twice — the second time by *removing* decoration.
- **OE-P11** — **interactive KPI cards** (Orders/Order-status filter in place; Dashboard KPIs deep-link) · **enhanced analytics dashboard** (cancellation + trash metrics, cancelled slice + total in the status donut, on-time radial gauge, horizontal pipeline bars) · **split-screen login redesign** · draft autosave, Order-status column picker, Neon `ws` driver fix.

---

## Implementation notes (living)
- **Next.js pinned to 15.x** per §2 (Next 15.5.x, React 19, Tailwind v4). Turbopack build.
- **Migrations work normally again.** The Neon DB had an empty `__drizzle_migrations` table, so `drizzle-kit migrate` always tried to replay from `0000_init` and failed on existing tables. The Supabase move rebuilt the schema from a fresh generated baseline, so the history and the database now agree. `db:generate` / `db:migrate` use **`DIRECT_URL`** (port 5432) — DDL must never go through the transaction pooler. `drizzle.config.ts` sets **`schemaFilter: ["ld_order_entry"]`**: without it drizzle-kit introspects sibling apps' schemas in the shared project and proposes to DROP their tables.
- **⚠️ `DATABASE_URL` must use port 6543 (TRANSACTION pooler), never 5432.** Session mode holds one backend connection per client for its whole lifetime and the Micro compute allows only 15, so a few warm serverless instances exhaust the pool and every query fails with `max clients reached in session mode` — presenting as a blanket 500 that says nothing about the cause. This took production down once after the cutover. `lib/db.ts` now logs a loud error if it sees 5432 in production. **`DIRECT_URL` is the one that belongs on 5432**, and it is used only by drizzle-kit, never at runtime.
- **Local dev runs against `ld_order_entry_dev`**, a structurally identical schema in the same Supabase project, so `npm run dev` and `db:seed` cannot write into live orders. Set with `DB_SCHEMA` in `.env.local`. Two guards make this safe: `db/schema.ts` **ignores `DB_SCHEMA` when `NODE_ENV=production`**, so a stray variable on Vercel can never redirect the live app; and `drizzle.config.ts` imports `db/force-prod-schema.ts` **before** drizzle-kit loads the schema, so generated migrations always carry the production schema name. Refresh the dev schema by re-applying the baseline DDL with the schema renamed.
- **`db/clone-to-dev.ts`** — copies the live order book into `ld_order_entry_dev` so `npm run dev` has real data to render (the dev schema starts empty, which makes every screen look broken). Reads production, writes ONLY to the dev schema, refuses to run when `DB_SCHEMA` is production, and is re-runnable (`ON CONFLICT DO NOTHING`).
- **`db/verify-write-path.ts`** — end-to-end write test (transaction, 7-stage seeding, stock gate, generated column, boolean persistence, cascade delete). It removes everything it creates, so it is safe to run against production and is the way to verify a write-path change without a local database.
- **Drizzle's journal is baselined.** `drizzle.__drizzle_migrations` on Supabase holds one row for `0000_boring_joseph` (hash = sha256 of the migration file, `created_at` = its `when` from `_journal.json`). Without it `db:migrate` would try to replay the baseline and fail on `CREATE SCHEMA` — the exact failure that made migrations unusable on Neon. Verified: `npm run db:migrate` exits clean.
- **`db/copy-to-supabase.ts` is a spent one-off and TRUNCATES its target.** It refuses to run unless `MIGRATE_ALLOW_WIPE=yes` is set, because the target is now live production.
- **Migrations to date:** `0000_boring_joseph` — the single baseline that creates the `ld_order_entry` schema, the `user_role` enum and all 8 tables. The Neon-era `public`-schema migrations `0000_init`–`0006_drop_manager_role` are kept for history under `db/migrations/_archive_neon_public/` and are never replayed.
- **Order/status/derivation logic** lives only in `lib/workflow.ts`: `applyStageProgress` + `WorkflowError` (§6), `computeLineStatus`, `computeOrderStatus`, `isOrderCancelled(total, cancelled)`, `isOrderDeleted(total, deleted)`, `plannedAtForOffset`, `buildInitialStageRows`, `lineMatchKey`.
- **Cancellation** — endpoint `app/api/orders/[id]/cancel/route.ts`; validation `cancelOrderSchema`; UI in `components/orders/order-detail.tsx`, the expandable Orders table (`orders-dashboard.tsx` + `order-designs.tsx` — `useDesignActions` hook), and struck rendering in the Order-status board.
- **Soft-delete + Trash** — endpoints `app/api/orders/[id]/delete/route.ts` (soft), `app/api/orders/[id]/lines/[lineId]/route.ts` (per-line purge), `DELETE app/api/orders/[id]/route.ts` (order purge, guarded), `app/api/trash/route.ts` (listing). Types `TrashOrder`/`TrashDesign`/`TrashList` in `lib/orders.ts`. Page `app/(app)/trash/page.tsx` + `components/trash/trash-view.tsx` (desktop tables + mobile cards). Nav: Trash is `orders.edit`-gated, nested under Settings in `NAV_ITEMS` but surfaced top-level for a non-admin editor who can't see Settings (`visibleNav`, `lib/rbac.ts`).
- **Orders dashboard** (`components/orders/orders-dashboard.tsx`): the list **fetches the full matching set** (`all=1`) and paginates client-side, so the **5 KPI cards are accurate all-orders counts AND one-click status filters** (Total / Completed / In progress / Pending / **Cancelled = designs across orders with a cancellation**). Rows are **expandable** (chevron) → `OrderDesignsPanel` (desktop) with per-design cancel + soft-delete; the mobile popup uses `OrderDesignsList`. Whole-order trash icon = **soft** delete (confirm dialog). Columns: Order no · Date · Party · Haste · Agent · Fabrics · Designs (active, `+N` cancelled hint) · Total Qty · Total Amount · Challan · Lot · Status · Actions.
- **Order Status** module (`components/order-status/*`, `lib/order-status.ts`, `app/api/order-status`): read-only board grouped **by order**. The grouping, the KPI counts, the overall/stage/cancelled refinement and the pagination all happen **on the server** (`lib/order-status-query.ts`, `loadOrderStatus`), using the same `aggregateOrderGroups` the client used to call — the board previously asked for every line with `all=1` and rolled ~4,100 of them up in the browser, a 5 MB response per filter change (now ~500 KB for a page of 20 orders). `all=1` still returns every group, and is now used only by the CSV export. Expandable rows; cancelled child rows struck, fully-cancelled parents tagged "Cancelled"; a **column picker** (`column-picker.tsx`, `useColumnPrefs`, per-user localStorage). KPI cards filter in place (Total / In progress / Completed / Overdue / **Cancelled = orders with a cancellation**) — these now round-trip to the server, which applies them at ORDER level (they used to be applied per LINE there, which is why the board ignored them); initial filter can be **deep-linked** via `?overall=` / `?cancelled=1` / `?stage=` (page wraps the board in `<Suspense>` for `useSearchParams`). A fully-cancelled group's `currentStageKey` is `null` so it never leaks into the "At stage" filter. Detail drawer `status-drawer.tsx` + CSV export stay line-level; deleted lines excluded, cancelled shown.
- **Order tracking view** (`components/order-status/order-tracker.tsx` + `tracker-detail.tsx` + `quality-groups.ts`, switched by `view-switch.tsx`): the "where is this order?" screen, offered on **both** Order status and Orders behind a Tracking/Board toggle — the existing board and orders table are untouched behind it, and the choice is remembered per user in localStorage. It carries the **same column filters as the Orders table** (`OrderFilters` — order no / challan / lot / haste / month / date range) behind a Filters button, and the **status key doubles as a filter**: clicking Completed / In progress / Not started / Cancelled narrows the rows, with a count on each. That filter is applied to the page in hand — the server paginates by order and the tones are per QUALITY, so it cannot be pushed down. Search order no or party → matches grouped **by QUALITY** (order 1135 becomes 4 rows, not 34) → the selected design's full status on the right, with ← / → (and the arrow keys) walking every match. **Each quality row carries its colours as chips**, green = dispatched, amber = not, so "which colours went out?" needs no second click; `isDispatched()` reads the `dispatch` stage. Order no / Party / Quality are **sticky** while the rest scrolls sideways. The table keeps the **full width** — the detail panel is a **floating window** (520px, `position: fixed`), shown only once a row is clicked, so nothing is resized and whatever it covers stays reachable by scrolling the table. **Drag it by its title bar** to anywhere on screen (double-click the bar to snap back); Esc or ✕ closes it. The panel opens with a **two-up facts grid in plain ink** — OD date, order no, sales person, quality, design no, mtr/yard, value, waiting on (challan and lot only when set) — deliberately uncoloured so it does not compete with the status below it. Then the **stage timeline** — the seven operations stages, each showing its **due date** and, once done, the **date and time** it was completed plus how late it ran — then **Whole order** (qualities, designs, total qty, total value, designs dispatched). The stages come first deliberately: an earlier version listed agent / department / haste / entered-on above them and pushed the actual tracking off the bottom of the panel. Rows that would only ever read "—" are not rendered. `StageCell` gained `plannedAt` / `delayMinutes` to feed this; both were already computed in `computeStages` but only reached the single-line detail endpoint. Search runs **as you type** (200 ms debounce, with a spinner and a live result count in the field, so a slow round trip never reads as "press Enter"). **Enter steps to the next design**, Shift+Enter back — skipped whenever focus is in a field or on a control. **The table follows the panel**: moving with ← / → / Enter (or the buttons) opens that quality — and collapses whichever was open, so exactly one is expanded at a time — and scrolls the row into view (`revealLine`, `block: "nearest"` so the table moves as little as possible), which keeps the row and the panel side by side. Its ⌖ button does the same but **centres and flashes** the row — expanding that quality if needed — and flashes the row, because repeated Next clicks walk the panel far from wherever the page happens to be scrolled. **Status is a plain-words column**, and the status colour is carried by the text of the order no, party, quality, design and mtr, derived from the stages already recorded (`toneOfLines`): green = every stage of every design done, orange = something started, red = nothing started, grey = all cancelled — plus a plain-words Status column. Tinting whole row backgrounds was tried and reverted: most work is unfinished, so the screen became a wall of red with the data buried under it. The panel header keeps a light tint and follows Previous/Next. Each of the seven stages has **its own column** (`stage-cell.tsx`, short headers — Entry / Stock / Rolling / Challan / Bill / Dispatch / LR, full names on hover), and its cell shows how many of that quality's designs are through it (`3/8`, `✓` when all). An earlier version packed them into one cell as chips (labels straight from `workflow_stages`, so renaming one in Settings renames it here) with an N/M count across a quality's designs — an earlier version showed a strip of design numbers, which read as noise. The design numbers stay where they mean something: the expanded per-colour rows and the panel's "Colours in …" list, where ✓ / • is dispatched / not. NOTE: this app has no colour field — what operators call a "colour" is `design_no` (the DSGN-MATCHING column in the old AppSheet, e.g. `21288-A` under `INDIANA CHECKS`); `remarks` is empty on every line. Reuses `GET /api/order-status` unchanged.
- **Tracking board** (`components/tracking/tracking-board.tsx`): the 7-stage grid, one row per (non-cancelled, non-deleted) line; §9 cell colours; per row only the **live** and **last-done** cells are editable; `stock_checking` is a Pending/In stock/Out of stock dropdown; header per-column prerequisite-aware "mark all"; a colour legend strip. Reads `GET /api/orders/:id/tracking`; writes `PATCH /api/tracking/stage` (→ 409 on rule/cancel/delete).
- **Analytics dashboard** (`components/dashboard/dashboard-view.tsx` + `dashboard-charts.tsx`, `app/api/dashboard`, `lib/dashboard.ts`): date-range + department filter; every aggregate **excludes cancelled AND soft-deleted** lines (and fully-deleted orders via `EXISTS(non-deleted line)`). Charts (Recharts + SVG): **KPI row** (6, each deep-links to a filtered list) · **operations pipeline** as horizontal clickable bars (→ `/tracking?stage=`) · **order trend** area (orders/value toggle) · **status split** donut (Completed/Partially/Pending/**Cancelled** + total in the hole) · **on-time delivery** radial gauge (green ≥90 / amber ≥70 / red) · **Cancellations & Trash** panel (cancelled designs/orders in range + Trash counts, links to `/trash`) · top parties/fabrics · recent orders · needs-attention.
  **The aggregation lives in `lib/dashboard-query.ts`** (`loadDashboard`), shared by the API route and by `app/(app)/page.tsx`, which prefetches it during SSR and hands it to the client query as `initial` — so the first paint carries numbers instead of skeletons, instead of waiting for the route's JS to download and hydrate before the request could even start. Filter changes still refetch through `/api/dashboard`. Roll-ups happen **in SQL**: the earlier version pulled ~13,000 rows per load (one per line item, plus one per overdue stage) to render seven bars and a ten-row list. Recharts is loaded with `next/dynamic`, and `OnTimeGauge` — raw SVG, no charting library — sits in its own file so it stays out of that chunk (route JS 300 kB → 186 kB). Two knock-on changes: `recentOrders` and `attention` now break same-date / same-day ties deterministically (by `order_no`, and by exact `planned_at` rather than whole days overdue), where before the winners fell out in database row order.
- **Auth & access:** Google sits alongside credentials in `lib/auth.ts` — the Google provider is only wired when `AUTH_GOOGLE_ID`/`AUTH_GOOGLE_SECRET` are set (the login page hides the button otherwise; on Vercel these must be set in the **Production** env, and the OAuth client's redirect URI must include the prod `…/api/auth/callback/google`). A `signIn` callback restricts Google to **existing active users** (no auto-provision). Capabilities are resolved from `role_permissions` in the `jwt` callback and stored on the JWT (`session.user.caps`); the edge middleware needs no DB. Write APIs use `requireCapability`; pages pass `caps` to components which gate with `hasCap`. Matrix changes apply on **next login** (session falls back to `DEFAULT_ROLE_CAPS` so a deploy never locks anyone out).
- **Login** — `app/(auth)/login/`: server `page.tsx` (split-screen, session-redirect + safe callbackUrl + googleEnabled), client `login-form.tsx` (credentials + Google + password show/hide + "Forgot password?" reveals an admin-reset hint, since there's no self-serve reset), `login-theme-toggle.tsx` (segmented Light/Dark via next-themes). The brand panel names three plain product statements rather than a row of stage chips — the chips wrapped onto a second line and left connector dashes dangling off each row. **Dark-mode inputs are deliberately SUNKEN here** (`dark:bg-[#0e1014]` against the panel's `surface` `#16181c`) instead of the global `surface-2` `#1d2025`, which is *lighter* than the surface behind it — a raised field on a dark canvas is the generic-dark-mode tell. Verified by luminance: dark 0.0051 < 0.0091, light 0.921 < 1.0. Scoped with `dark:` variants so the app's own tokens are untouched. Auth behaviour has never changed through any of the visual passes.
- **Order entry form** (`components/orders/order-form.tsx`): rich header + fabric blocks × designs; **draft autosave** to `localStorage` (`oe:new-order-draft:v1`, create mode only) so a refresh keeps typed data; bulk "add N designs"; live duplicate `order_no` check.
- **`components/ui/h-scroll.tsx` (`HScroll`) — every wide table's horizontal scrollbar.** A table wider than the screen puts its scrollbar at the BOTTOM, so on a long list you scroll past every row to reach it. `HScroll` adds a second scrollbar **directly above the header row**, kept in sync with the real one both ways, and only rendered when the content actually overflows. `.h-scrollbar` in `globals.css` styles both so they are visible rather than the thin overlay bars some platforms default to. Applied to the tracker, Order-status board, Orders table, Operations board + index, Trash, Design DB, Users, Access and the Dashboard's monthly report. Bound the body's height (`max-h-…` on `bodyClassName`) so the bottom bar stays on screen too, and pin the `<thead>` when you do.
- **`components/ui/pager.tsx` (`Pager`) — every paginated list's footer.** Previous / a typed page box / Next: with 13 pages, walking to page 11 was ten clicks and ten refetches, so the page number is an input — type it, press Enter. Clamps to 1..totalPages, reverts on Escape or gibberish, selects on focus, and follows the real page when a filter resets it. Used by the tracking view, Order-status board, Orders table, Operations index and the Design DB.
- **Shared table primitives** live in `components/ui/table.tsx` (`Th`, `THead`) — reuse for new tables. Shared KPI tile `components/ui/stat-card.tsx`.
- **⚠️ Never fan out more concurrent queries than the pool holds.** `lib/db.ts` caps postgres.js at `max: 5`. Issuing more than that at once through the Supavisor transaction pooler does **not** simply queue: the surplus waits on connections the pooler has already rotated, and the request **stalls for minutes** or dies with `CONNECTION_CLOSED` / `canceling statement due to statement timeout`. Reproduced against the live pooler — the first call in a warm process succeeds and the very next one hangs, which is exactly how a warm serverless instance behaves. This is what made the Dashboard slow: it fired 14 queries in one `Promise.all`. `lib/dashboard-query.ts` now fans out in **waves of ≤4**. Any new endpoint that wants many independent queries must do the same. Note the blast radius: `lib/db.ts` is a **single shared pool**, so one endpoint wedging it leaves *every* screen in that server process stuck on "Loading…" — which is how a slow Dashboard presented as a slow Orders / Order status / Operations.
- **One database client, not two.** Neon needed an HTTP driver for reads plus a WebSocket pool for transactions; postgres.js does both, so `lib/db.ts` exports a single instance and `dbx` is just an alias for `db` (kept so the six transaction call sites are unchanged). The old `serverExternalPackages` / `bufferutil` workaround in `next.config.ts` is gone with Neon.
- **Connection strings.** Runtime uses the **Supavisor transaction pooler (port 6543)** with `prepare: false` — required, because the pooler hands a different backend connection to each transaction. DDL uses `DIRECT_URL` (port 5432). The direct host `db.<ref>.supabase.co` is **IPv6-only**; use the pooler from Vercel and from IPv4-only networks.
- **Exports:** `/api/export/orders` is JSON (§7); Order-Status and Orders "Export" build CSV client-side. SheetJS/`xlsx` reserved, not yet wired.
- **Local env:** `.env.local` (gitignored) holds `DATABASE_URL` (Supabase **transaction pooler**, port 6543), `DIRECT_URL` (port 5432, migrations only), `AUTH_SECRET`, `AUTH_GOOGLE_ID`/`AUTH_GOOGLE_SECRET` (blank = Google hidden), `EXPORT_API_KEY`, `NEXT_PUBLIC_APP_VERSION`. Vercel production keeps its own env. `db:seed` seeds the 7 stages, sample lookups, one ADMIN, and the default `role_permissions` matrix. **Point local dev at a non-production database** — the Supabase project is the live system of record.
